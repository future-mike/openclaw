---
description: OpenClaw 项目整体介绍，供每次问答参考
alwaysApply: true
---

# OpenClaw 项目介绍

回答项目相关问题时，请优先基于下方内容作答并保持一致。

## 一、项目概述

- **名称**：OpenClaw
- **简介**：在自有设备上运行的**个人 AI 助手**。通过已有渠道（WhatsApp、Telegram、Slack、Discord、Google Chat、Signal、iMessage、Microsoft Teams、WebChat 等）与你对话，支持语音、Canvas 画布、多 Agent 路由。Gateway 是控制面，产品形态是助手。
- **目的/目标**：提供本地优先、快速、常驻的个人单用户助手；统一多通道收件箱与工具（浏览器、Canvas、节点、Cron、会话等）。
- **技术栈**：Node ≥22、TypeScript、pnpm；CLI 入口 `openclaw.mjs`；构建产物 `dist/`；可选 macOS/iOS/Android 原生应用。
- **主要功能**：Gateway WebSocket 控制面、多通道收件箱、Pi Agent RPC 运行时、Voice Wake / Talk Mode、Live Canvas (A2UI)、浏览器/节点/Cron/会话工具、向导式入驻（onboard）、Skills 平台（ClawHub）。

## 二、本地运行方式

1. **环境**：Node ≥22，推荐 pnpm（或 npm/bun）。
2. **安装**：`pnpm install`；首次构建 UI：`pnpm ui:build`。
3. **构建**：`pnpm build`（产出 `dist/`）。
4. **入驻**：`pnpm openclaw onboard --install-daemon`（向导配置 Gateway、工作区、通道、Skills，并安装守护进程）。
5. **开发**：`pnpm gateway:watch`（TS 变更自动重载）；或直接运行：`pnpm openclaw gateway --port 18789 --verbose`。
6. **访问**：Gateway 默认 `ws://127.0.0.1:18789`；Control UI / WebChat 由 Gateway 提供。

## 三、目录结构及各目录作用

| 目录/文件 | 作用 |
|-----------|------|
| `src/` | 核心 TypeScript 源码：Gateway、CLI、Agent、通道、Web、配置等 |
| `src/gateway/` | Gateway WebSocket 服务、会话、配置、Cron、Webhook |
| `src/agents/` | Pi Agent 运行时、RPC、工具调用与流式输出 |
| `src/channels/` | 各通道实现（WhatsApp、Telegram、Slack、Discord 等） |
| `src/cli/` | CLI 命令（gateway、agent、send、wizard、doctor 等） |
| `src/config/` | 配置解析与校验 |
| `src/web/` | WebChat、Control UI、登录/会话/媒体等 Web 逻辑 |
| `src/wizard/` | 入驻向导（onboard） |
| `apps/` | 原生应用：macOS 菜单栏、iOS、Android（Swift/Kotlin） |
| `docs/` | 官方文档站点（Mint 等） |
| `extensions/` | 插件/扩展（如频道扩展） |
| `skills/` | 内置与示例 Skills（SKILL.md 等） |
| `ui/` | Control UI 前端工程 |
| `scripts/` | 构建、测试、协议生成、打包等脚本 |
| `test/` | 单元/集成/E2E 测试 |
| `packages/` | 内部 npm 包 |
| `assets/` | 静态资源、Chrome 扩展、DMG 背景等 |
| `dist/` | 构建输出（运行与发布用） |
| `.github/` | CI、issue 模板、Dependabot 等 |
| `openclaw.mjs` | CLI 入口；`pnpm openclaw` 即通过此文件调用 |

## 四、工作流程与目的

- **推荐上手**：运行 `openclaw onboard` 完成 Gateway、工作区、通道、Skills 配置；CLI 向导为推荐路径（支持 macOS/Linux/Windows WSL2）。
- **开发流程**：拉代码 → `pnpm install` → `pnpm ui:build`（首次）→ `pnpm build` → `pnpm openclaw onboard --install-daemon` → `pnpm gateway:watch` 进入开发环；`pnpm openclaw ...` 通过 tsx 直接跑 TS。
- **发布/分支**：stable（tag 发布）、beta（预发布）、dev（main 头）；切换通道：`openclaw update --channel stable|beta|dev`。
- **流程目的**：保证本地可复现、CI 通过、文档与配置一致；安全默认（DM 配对、沙箱可选）见 [Security](https://docs.openclaw.ai/gateway/security)。

---

*详细说明见项目根目录 `README.md` 与 [docs.openclaw.ai](https://docs.openclaw.ai)。*
